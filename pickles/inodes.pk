deftype inode =
  struct
  {
    uint32 perms;
    uint32 size;
    byte[size] data;
    uint32 next;
  };

defproc print_inodes (inode in)
{
  print ("Perms: " + in.perms);
  print_inode_data (in.data);

  if (next != 0)
    print_inodes (inode @ in.next);
}

/*
Local variables:
mode:c
End:
*/
