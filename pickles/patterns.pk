/* patterns.pk - Examples of patterns.  */

/* Note that in order to locate patterns efficiently, alignment
   constraints should be specified in types.  */

deftype retpoline =
  struct
  {
    ind_jump jump1;
  };
